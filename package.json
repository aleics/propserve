{
  "name": "observer",
  "version": "0.0.1",
  "description": "",
  "author": "Aleix Casanovas (aleics)",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "ci": "npm run build && npm run test"
  },
  "jest": {
    "preset": "ts-jest",
    "moduleNameMapper": {
      "^observer/(.*)$": "<rootDir>/src/$1"
    }
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "rxjs": "^6.5.3"
  }
}
